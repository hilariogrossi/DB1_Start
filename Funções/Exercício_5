# Escreva um programa que execute uma função que calcule o fatorial de um número passado por parâmetro.
from math import factorial


def fatorial(numero):
    resultado = factorial(numero)
    print(f'O resultado do fatorial do número {numero}! é igual a {resultado}.')


fatorial(numero = int(input('Digite o número a ser fatorado: ')))
